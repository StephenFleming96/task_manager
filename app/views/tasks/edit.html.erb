
<%= render 'form', hide_start: true %>

<!--
<div class="container">
	<h1>Edit Task</h1>

	<% 
		_taskURL = "/tasks/#{@task.id}"
		_task_stat = @task.status.to_i
	%>
	
	<%= form_for @task, url: _taskURL, method: 'PUT'  do |form| %>
		<% if @task.errors.any? %>
			<div>
				<h2>
					<%= pluralize(@task.errors.full_messages.count, 'error') %> prevented the form from saving!
					<ul>
						<% @task.errors.full_messages.each do |msg| %>
							<li><%= msg %></li>
						<% end %>
					</ul>
				</h2>
			</div>
		<% end %>
		
		<p>
			<%= form.label :title %>
			<br/>
			<%= form.text_field :title %>
		</p>

		<p>
			<%= form.label :description %>
			<br/>
			<%= form.text_field :description %>
		</p>

		<p class='form-select'>
			<%= form.label :status %>
			<br/>
			<%= render '/tasks/dropdown', default_status: _task_stat %>
		</p>

		<p class='form-select'>
			<%= form.label :end %>
			<br/>
			<%= form.datetime_select :end%>
		</p>

		<p>
			<%= form.submit %>
		</p>
	<% end %>
</div>
-->